{
  "name": "panna-cotta-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "tauri-driver smoke tests for Panna Cotta (Linux only — macOS support is broken upstream)",
  "scripts": {
    "smoke": "wdio run wdio.conf.ts",
    "smoke:headed": "wdio run wdio.conf.ts"
  },
  "devDependencies": {
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "webdriverio": "^9.0.0"
  }
}
