import React from 'react'
import ReactDOM from 'react-dom/client'
import './app.css'
import { PannaApp } from './PannaApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PannaApp />
  </React.StrictMode>
)
